server:
  port: 8082

spring:
  application:
    name: sellers-service

  datasource:
    url: jdbc:postgresql://localhost:5432/comprago_sellers
    username: comprago
    password: comprago123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
